/*
      <ul>
        { this.state.persons.map(person => (
          <div><li>{person.name}</li><li>{person.price}
          </li></div>
        ))}
      </ul>

      <ul>
        { this.state.persons.map(person => (
          <div><li>{person.name}</li><li>{person.price}
          </li></div>
        ))}
      </ul>
*/